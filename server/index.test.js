const app = require('./index');

describe('API test calls', () => {
  it('should return array of makes greater than 2', async (done) => {

  });
  
  // it('should return array of models greater than 2 for make: Acura', async () => {
  //   await request(app)
  //   .get("/models?selectedMake=acura")
  //   .expect(200)
  //   .then((response) => {
  //     expect(response.body.lenght).toBeGreaterThan(1);
  //   })
  //   done();
  // });
});